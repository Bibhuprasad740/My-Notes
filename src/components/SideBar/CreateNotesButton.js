import React from "react";
import classes from "./CreateNotesButton.module.css";

const CreateNotesButton = (props) => {
  return <button className={classes.button}>+ Create Notes Group</button>;
};

export default CreateNotesButton;
